# Optional AI provider (OpenAI-compatible) for the `ai:<prompt>` binding.
# These can also be set in-app via the gear (Settings) dialog.
VITE_AI_BASE_URL=https://api.openai.com/v1
VITE_AI_API_KEY=
VITE_AI_MODEL=gpt-4o-mini
